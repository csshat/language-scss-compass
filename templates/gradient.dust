@include background-image(
  {#gradient}{type}-gradient({angle|gradientAngle}
    {#stops}
        , {@color type=options.colorType}{color}{/color} {location}%
    {/stops}
    ){@sep}, {/sep}
  {/gradient});{~n}
